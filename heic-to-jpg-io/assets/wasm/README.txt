This folder is a placeholder for libheif WASM binaries when self-hosting.

This project currently loads a bundled libheif WASM build from a CDN (lazy loaded on first conversion),
so no files are required here by default.

